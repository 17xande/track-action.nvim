-- track-action.nvim plugin initialization
-- This file is automatically loaded by Neovim

-- Prevent loading twice
if vim.g.loaded_track_action then
  return
end
vim.g.loaded_track_action = 1

-- Plugin will be set up by user calling require("mentor").setup()
-- This file just marks the plugin as loaded
